| Area                                                                                                                           | Status             | Why it matters                      | What must be true to call it “ready”                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **Ports (HTTP)**: `/k0/command.submit`, `/k0/query.recall`, `/k0/sse.subscribe`, `/k0/sse.ack`, `/k0/obs.emit`, health/metrics | **GREEN** (ready)  | All traffic enters here             | All 8 HTTP endpoints validated with 28 comprehensive tests covering 200/4xx/5xx paths, error handling, robustness via pytest integration test (tests/k0/integration/test_http_ports_integration.py: all 28 tests passing) |
| **Minimal Gate** (envelope+hash, schema lookup, provisioning, idempotency check)                                               | **GREEN** (design) | First line of defense               | Active schema registry; provisioning ledger has at least one device/key; idem ledger TTL policy configured              |
| **PEP (policy)**                                                                                                               | **GREEN** (ready)  | Prevents unsafe/unauthorized writes | PEP policy evaluation engine with 26 comprehensive tests covering allow/deny/redact paths, role-based access control, topic wildcards, band enforcement, obligation extraction, redaction directives, and concurrent evaluation — all 26 tests passing in tests/k0/integration/test_pep_policy_integration.py |
| **QoS (Scheduler + bands + budgets)**                                                                                          | **GREEN** (ready)  | Prevents overload/starvation        | Per-port token buckets + per-topic budgets; rejection path returns clear error; counters in metrics — all verified via pytest integration test (tests/k0/integration/test_qos_integration.py: 22 tests, all passing) |
| **UnitOfWork** (transaction spine)                                                                                             | **GREEN** (ready)  | Atomicity & ordering                | WAL append + Outbox stage + Receipt save + Idem upsert + on_commit hooks all verified atomically via pytest integration test (tests/k0/integration/test_transaction_spine_integration.py: 9 tests, all passing) |
| **WAL** (+ snapshot/replay/promotion)                                                                                          | **GREEN** (ready)  | Durability & recovery               | Snapshot created/restored; replay to target WAL position is deterministic; promotion tested — all 24 comprehensive tests in tests/k0/integration/test_wal_integration.py covering append/durability, snapshot creation, replay from position, tenant/space scoping, promotion lag, delta reconciliation, and robustness |
| **Receipts** (issuer/store)                                                                                                    | **GREEN** (ready)  | Proof and audit                     | Ed25519 signatures verifiable with 28 comprehensive tests; receipt ↔ WAL position linking validated; negative tests for tampering, invalid signatures, edge cases — all 7 test classes in tests/k0/integration/test_receipts_integration.py, 28 tests all passing |
| **Bus Dispatcher + Middleware**                                                                                                | **GREEN** (ready)  | Post-commit fan-out                 | Monotonic offsets enforced (6 tests); middleware chain execution validated (4 tests); fan-out to multiple sinks (6 tests); QoS token management (2 tests); error handling (6 tests); band resolution (3 tests); edge cases (6 tests) — all 36 tests passing in tests/k0/integration/test_bus_dispatcher_integration.py |
| **SSE Server + Offsets + Backpressure**                                                                                        | **GREEN** (ready)  | Live updates                        | Subscribe with ACL authorization (5 tests) + cursor validation (7 tests); acknowledge offset updates st_offsets (4 tests); backpressure levels (normal/warning/throttle/shed) progression validated (7 tests); cursor encode/decode robustness (7 tests); ACL patterns/wildcards (7 tests); multi-topic backpressure (2 tests); complete subscribe-ack cycle (1 test) — all 38 comprehensive tests passing in tests/k0/integration/test_sse_server_integration.py |
| **Outbox (store/worker/pool/retry/DLQ)**                                                                                       | **GREEN** (ready) | Asynchronous execution              | Success→Retry→DLQ paths with jittered exponential backoff; idempotent driver invoke by (driver, fingerprint); lease/heartbeat with expiry reclamation; poison-pill guard capping retries; metrics emission (success/retry/dlq counters, pending gauges); batch processing limits — all 12 comprehensive integration tests passing in tests/k0/integration/test_outbox_integration.py |
| **Driver SPI + AliasMap**                                                                                                      | **GREEN** (ready) | Pluggability                        | Real FtsDriver wired (st_fts table + FTS5 search); AliasDriver no-ops for unsupported types (vector, kg, episodic, snapshot); registry resolution with type matching; alias resolution logging; all 15 comprehensive integration tests passing in tests/k0/integration/test_driver_spi_integration.py |
| **Query Subsystem** (registry + drivers)                                                                                       | **GREEN** (ready)  | Multi-table recall                  | WAL driver returns canonical rows (11 fields: wal_pos, tenant_id, space_id, topic, commit_ts, schema_uri, schema_version, device_id, payload_sha256, envelope, body); FTS driver working; pagination cursor stable — all 22 comprehensive integration tests passing in tests/k0/integration/test_query_subsystem_integration.py covering registry resolver (6), WAL driver (5), FTS driver (2), pagination (3), multi-driver coordination (2), error handling (2), performance budgets (2) |
| **Observability** (metrics/traces/buffer)                                                                                      | **GREEN** (ready)  | Operability                         | `/metrics` exposes counters/gauges/histograms; obs buffer flushes; traces show trace_id from ports to bus — all 26 comprehensive integration tests passing in tests/k0/integration/test_observability_integration.py covering MetricsExporter (6 tests for counters/gauges/histograms), TracerFactory (5 tests for spans/trace_id/context), ForwardedMetricsBuffer (4 tests for snapshots/metadata), HTTP endpoint /k0/obs.emit (5 tests for batch ingestion/headers/errors), trace_id propagation (3 tests), performance budgets (2 tests latency <1ms), full integration flow (1 test) |
| **SQLite Runtime + Conn Pool + Schemas**                                                                                       | **GREEN** (design) | Storage foundation                  | WAL mode on; timeouts set; all tables exist; migrations reproducible via CLI                                            |
| **CLI subsystem** (`k0ctl`, automation)                                                                                       | **GREEN** (ready)  | Ops lifecycle                       | `migrate apply`, `lint_schemas`, `verify_docs_sync` all tested and passing — 20 comprehensive integration tests covering migrate command (6 tests: apply, dry-run, checksums, idempotency, errors), lint_schemas (4 tests: ref extraction, JSON pointer resolution, internal refs, broken refs), verify_docs_sync (3 tests: SQL fence extraction, statement normalization, front matter parsing), k0ctl parser (2 tests: migrate/schema commands), error handling (3 tests: value coercion, override pairs, composition), integration flows (2 tests: full migrate flow, provision args) in tests/k0/integration/test_cli_integration.py |
| **pytest tests & perf budgets**                                                                                              | **RED→AMBER**      | Safety net                          | Happy-path + failure-path pytest suites for command, query, SSE, outbox, replay; P95 latencies within targets            |
