# K0 Kernel - Minimal Dependencies
# UltraBERT replaces 9 separate models (4.6GB -> 500MB)
# Includes: k0/deploy/requirements.base.txt (observability, hashing, logging)

# Base dependencies (shared with all containers)
-r requirements.base.txt

# FastAPI and Server
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
starlette>=0.32.0

# Core Dependencies
neo4j>=5.20.0                      # Knowledge graph driver (Bolt protocol)
numpy>=1.26.0                      # Basic array operations (minimal)

# =============================================================================
# UltraBERT Unified Model (replaces ALL ML dependencies below)
# =============================================================================
# Install from wheel: pip install familyos_ultrabert-2.0.3-py3-none-any.whl
# Capabilities: sentiment, emotions, safety, NER, temporal, intent, embedding
# Size: 155M params, ~500MB memory
# See: wheels/README.md for installation instructions

# UltraBERT runtime dependencies
onnxruntime>=1.17.0                # ONNX Runtime backend for UltraBERT
tokenizers>=0.15.0                 # Fast tokenization for UltraBERT
transformers>=4.30.0               # HuggingFace transformers (tokenizer config)

# Minimal NLP for tokenization only (UltraBERT handles all ML)
spacy>=3.7.0                       # Tokenization and parsing only

# =============================================================================
# DEPRECATED - Replaced by UltraBERT
# =============================================================================
# vaderSentiment -> UltraBERT sentiment
# transformers -> UltraBERT (includes all pipelines)
# torch -> UltraBERT (includes ONNX runtime)
# sentence-transformers -> UltraBERT embedding

# JWT and Authentication
PyJWT>=2.8.0
cryptography>=41.0.0

# Performance & Utilities
tenacity>=8.0.0                    # Retry logic with backoff

# Validation & Config
pydantic>=2.0.0                    # Data validation
python-dotenv>=1.0.0               # Environment variables
jsonschema>=4.20.0                 # JSON schema validation
PyYAML>=6.0.0                      # YAML parsing
jinja2>=3.1.0                      # Template rendering
geohash2>=1.1                      # Location hashing (GDPR compliance)

# HTTP & Async
httpx>=0.27.0                      # Async HTTP client

# PostgreSQL (high-throughput concurrent writes)
asyncpg>=0.29.0                    # Async PostgreSQL driver
psycopg[binary]>=3.1.0             # Sync PostgreSQL driver (migration scripts)

# Vector similarity search (FAISS)
# P08 FAISS indexing for similarity search
# Note: Use faiss-gpu in GPU containers (installed via Dockerfile)
faiss-cpu>=1.8.0                   # CPU fallback for development

# Note: UltraBERT wheel installed separately - container size ~600MB (was 4GB)
