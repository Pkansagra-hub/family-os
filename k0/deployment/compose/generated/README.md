# Generated Compose Bundles

This directory is populated by the Pulumi programs under `k0/deployment/pulumi`
during `pulumi up` or artifact export workflows. Add this directory to
`.gitignore` if it is not already ignored.
