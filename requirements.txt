# FamilyOS Root Requirements
# This file contains ALL dependencies needed across all containers + development tools
#
# Container-specific breakdowns:
# - k0/deploy/requirements.kernel.txt (kernel container)
# - k0/deploy/requirements.embedding-worker.txt (embedding worker)
# - k0/deploy/requirements.fts-worker.txt (FTS worker)
# - k0/deploy/requirements.base.txt (shared by all containers)
# - k0/deploy/requirements.dev.txt (development & testing only)
#
# For production: use individual container requirements files
# For local development: pip install -r requirements.txt

# ==================== SHARED BASE (ALL CONTAINERS) ====================
# Logging
loguru>=0.7.0

# Serialization & Hashing
orjson>=3.9.0
blake3>=0.4.1

# Observability (Prometheus + OpenTelemetry)
prometheus-client>=0.18.0
opentelemetry-api>=1.24.0
opentelemetry-sdk>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0

# ==================== KERNEL CONTAINER ====================
# FastAPI and Server
fastapi>=0.108.0
uvicorn[standard]>=0.25.0
starlette>=0.32.0

# Core Dependencies
neo4j>=5.20.0
numpy>=1.26.0

# JWT and Authentication
PyJWT>=2.8.0
cryptography>=41.0.0

# Performance & Utilities
tenacity>=8.0.0

# Validation & Config
pydantic>=2.0.0
python-dotenv>=1.0.0
jsonschema>=4.20.0
PyYAML>=6.0.0
PyNaCl>=1.5.0
jinja2>=3.1.0
geohash2>=1.1

# HTTP & Async
httpx>=0.27.0
httpx-sse>=0.4.0

# PostgreSQL (asyncpg for async operations)
asyncpg>=0.30.0

# ==================== EMBEDDING WORKER CONTAINER ====================
# NLP & Embeddings - UltraBERT replaces most of these
# Install UltraBERT from wheel: pip install wheels/familyos_ultrabert-2.0.3-py3-none-any.whl
nltk>=3.8.1

# DEPRECATED - Replaced by UltraBERT
# sentence-transformers -> UltraBERT embedding
# textblob -> UltraBERT sentiment
# vaderSentiment -> UltraBERT sentiment

# ONNX Runtime (UltraBERT uses this internally)
onnxruntime>=1.17.0
tokenizers>=0.20.0

# String matching (used by entity_disambiguator)
rapidfuzz>=3.6.0

# OpenAI integration (optional)
openai>=1.12.0
tiktoken>=0.5.0

# ==================== FTS WORKER CONTAINER ====================
# (FTS only needs nltk from above, already listed)

# ==================== DEVELOPMENT & TESTING ====================
# Testing Frameworks
hypothesis>=6.98.0

# Infrastructure-as-Code
pulumi>=3.115.0
ansible-core>=2.16.0
ansible-runner>=2.3.5

# Documentation
fpdf2>=2.7.9

# Advanced NLP (optional, research only - NOT in production)
spacy>=3.7.0

# Vector Search (optional, research only - NOT in production)
faiss-cpu>=1.8.0
