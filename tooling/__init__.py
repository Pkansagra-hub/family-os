"""FamilyOS in-tree tooling.

Subpackages contain build-time code that is NOT imported by runtime modules.
Currently:

* :mod:`tooling.contracts` — bridge contract registry + codegen substrate
  (MS-2.5).
"""
