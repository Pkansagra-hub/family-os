# Rate Limiting Configuration
# Token bucket configurations per service

services:
  agent_creation:
    tokens_per_second: 10
    burst_size: 50

  model_inference:
    tokens_per_second: 100
    burst_size: 200

  data_retrieval:
    tokens_per_second: 50
    burst_size: 100
