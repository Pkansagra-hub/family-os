| Area                                                                                                                           | Status             | Why it matters                      | What must be true to call it “ready”                                                                                    |
| ------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ----------------------------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **Ports (HTTP)**: `/k0/command.submit`, `/k0/query.recall`, `/k0/sse.subscribe`, `/k0/sse.ack`, `/k0/obs.emit`, health/metrics | **AMBER**          | All traffic enters here             | Each port live with Ward tests + 200/4xx/5xx paths; basic auth/ACLs enforced where relevant                             |
| **Minimal Gate** (envelope+hash, schema lookup, provisioning, idempotency check)                                               | **GREEN** (design) | First line of defense               | Active schema registry; provisioning ledger has at least one device/key; idem ledger TTL policy configured              |
| **PEP (policy)**                                                                                                               | **AMBER**          | Prevents unsafe/unauthorized writes | OPA/Cedar/RBAC policies loaded + obligation handling; at least 3 rules tested (allow/deny/redact)                       |
| **QoS (Scheduler + bands + budgets)**                                                                                          | **AMBER**          | Prevents overload/starvation        | Per-port token buckets + per-topic budgets; rejection path returns clear error; counters in metrics                     |
| **UnitOfWork** (transaction spine)                                                                                             | **GREEN** (design) | Atomicity & ordering                | WAL append + Outbox stage + Receipt save + Idem upsert + on_commit hooks all verified atomically                        |
| **WAL** (+ snapshot/replay/promotion)                                                                                          | **AMBER**          | Durability & recovery               | Snapshot created/restored; replay to target WAL position is deterministic; promotion tested                             |
| **Receipts** (issuer/store)                                                                                                    | **AMBER**          | Proof and audit                     | Ed25519 signatures verifiable; receipt → WAL position resolvable; negative tests for tamper                             |
| **Bus Dispatcher + Middleware**                                                                                                | **AMBER**          | Post-commit fan-out                 | Monotonic offsets enforced; middleware emits metrics; ordering verified across two subscribers                          |
| **SSE Server + Offsets + Backpressure**                                                                                        | **AMBER**          | Live updates                        | Subscribe → stream → ack advances `st_offsets`; throttle engages under synthetic load                                   |
| **Outbox (store/worker/pool/retry/DLQ)**                                                                                       | **AMBER**          | Asynchronous execution              | Batch dequeue → driver invoke → success + retry + DLQ paths; jitter/backoff verified; single DLQ table (no duplication) |
| **Driver SPI + AliasMap**                                                                                                      | **AMBER**          | Pluggability                        | At least one real driver wired (e.g., `st_fts` or `st_vector`) + one no-op driver; alias resolution logged              |
| **Query Subsystem** (registry + drivers)                                                                                       | **AMBER**          | Multi-table recall                  | WAL driver returns canonical rows; optional FTS/vector plugged; pagination cursor stable                                |
| **Observability** (metrics/traces/buffer)                                                                                      | **AMBER**          | Operability                         | `/metrics` exposes counters/gauges/histograms; obs buffer flushes; traces show trace_id from ports to bus               |
| **SQLite Runtime + Conn Pool + Schemas**                                                                                       | **GREEN** (design) | Storage foundation                  | WAL mode on; timeouts set; all tables exist; migrations reproducible via CLI                                            |
| **CLI subsystem** (`k0ctl`, automation)                                                                                        | **AMBER**          | Ops lifecycle                       | `migrate apply`, `lint_schemas`, `verify_docs_sync` succeed in CI                                                       |
| **Ward tests & perf budgets**                                                                                                  | **RED→AMBER**      | Safety net                          | Happy-path + failure-path Ward suites for command, query, SSE, outbox, replay; P95 latencies within targets             |
