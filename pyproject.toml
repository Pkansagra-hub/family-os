[tool.ward]
path = ["tests", "k1/tests"]

[tool.ward.ini_options]
# Disable OpenTelemetry export during tests
addopts = "--tb=short"
env = [
    "OTEL_TRACES_EXPORTER=none",
    "OTEL_METRICS_EXPORTER=none",
]

