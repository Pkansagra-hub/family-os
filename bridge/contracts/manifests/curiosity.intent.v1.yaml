topic: curiosity.intent.v1
direction: k0_to_k1
owner_team: k0
status: active
producer:
  kernel: k0
  pipeline: p06.curiosity
  emitter: CuriosityEmitter
consumer:
  kernel: k1
  pipeline: concierge.proactive_listener
  state: gap_sse_listener
schema: schemas/curiosity.intent.v1.json
delivery:
  transport: sse
  ordering: best_effort
  ack_required: true
  online_required: false
  endpoint_class: cloud_k0
  partition_mode: k0_primary
  codec: json
  retry: exponential_backoff
  max_redelivery: 0
semantics:
  idempotent: true
  duplicate_strategy: dedupe_by_envelope_id
  retention: PT24H
  description: 'P06 Active-Learning curiosity question pushed to K1 over SSE. Cursor
    resume via Last-Event-ID (D13); duplicates on replay are deduped by envelope_id.
    24h replay window matches the SSE replay-buffer retention. Online_required=false
    because the concierge tolerates K0 outage by simply not receiving events; the
    cursor resumes on reconnect.

    '
sla:
  latency_p99_ms: 500
  availability: degrades_to_local_queue
versioning:
  semver: 1.0.0
  compat: compatible
  strategy: additive_only_within_v1
  breaking_change_requires: new_topic_v2_with_dual_publish_window_30d
checksums:
  schema_sha256: c204fb998ddf79e244207f6c22cc4a0693c3dac614cfdcf2837e432063f1b711
  manifest_sha256: 359cdd05d60a1fe5de30bfd5d58f3693915cfc0483d1f720518e4aba0960cc08
